A simple web app to convert the color space of textures in .GLB 3D models from RGB to Linear and compress the output.
Live demo: <https://augment.ing/3D-Converter.html>
## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
